import shopComponent from "@/pages/shopComponent.vue";
import ShoppingCart from "@/pages/shoppingCart.vue";

const routes = [
    { path: '/', component: shopComponent },
    {path : "/shoppingCart" , component : ShoppingCart }
  ];
  export {routes}